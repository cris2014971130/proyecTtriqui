public class Attribute{
    
    public enum shape {SQUARE, CIRCLE, TRIANGLE};
    public enum size {BIG, MEDIUM, SMALL};
    public enum color {BLACK, GREEN, RED};
    public enum equis {HAVE, NO_HAVE};

}

